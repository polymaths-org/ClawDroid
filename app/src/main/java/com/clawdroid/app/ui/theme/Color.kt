package com.clawdroid.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Core dark palette ──────────────────────────────────────
val DeepBlack       = Color(0xFF05070A)   // Extra deep midnight black
val NightSurface    = Color(0xFF0A0F18)   // Deep navy surface
val CardDark        = Color(0xFF101726)   // Glass card background
val ElevatedDark    = Color(0xFF141D30)

// ── Blue & Cyan theme accents (backward compatible names) ──
val FireRed         = Color(0xFF00F2FE)   // Neon Cyan
val EmberOrange     = Color(0xFF4FACFE)   // Neon Blue
val MoltenYellow    = Color(0xFF00C6FF)   // Electric Blue
val LavaGlow        = Color(0xFF00C6FF)
val DeepEmber       = Color(0xFF08203E)   // Deep Navy

// ── Specific Blue & Cyan declarations ──────────────────────
val NeonBlue        = Color(0xFF4FACFE)
val NeonCyan        = Color(0xFF00F2FE)
val ElectricBlue    = Color(0xFF00C6FF)
val DeepNavy        = Color(0xFF050B14)
val ActivePurple    = Color(0xFF9D4EDD)

// ── Text & semantic ────────────────────────────────────────
val SoftWhite       = Color(0xFFF0F4F8)
val MutedGray       = Color(0xFF6B7C96)   // Sleek slate gray
val DimGray         = Color(0xFF3B4E68)
val ErrorRed        = Color(0xFFEE4444)

// ── Glassmorphic fills ─────────────────────────────────────
val GlassFill       = Color(0x1400C6FF)   // 8% cyan fill
val GlassFillMedium = Color(0x2400C6FF)   // 14% cyan fill
val GlassFillStrong = Color(0x3800C6FF)   // 22% cyan fill
val GlassBorder     = Color(0x3300F2FE)   // 20% neon cyan border
val GlassBorderDim  = Color(0x1F00F2FE)   // 12% neon cyan border dim

// ── Voice colors ───────────────────────────────────────────
val UserVoiceBlue   = Color(0xFF00C6FF)
val UserVoiceCyan   = Color(0xFF00F2FE)
val AgentVoiceRed   = Color(0xFF9D4EDD)   // Purple for Agent speaking
val AgentVoiceGold  = Color(0xFF7B2CBF)
